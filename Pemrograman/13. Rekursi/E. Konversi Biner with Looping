#include<stdio.h>

int main(){
    int N;
    scanf("%d", &N);
    int simpan[64];
    int i = 0;
    while(N > 0){
        simpan[i] = N % 2;
        N /= 2;
        i++;
    }
    for(int j = i-1; j >= 0; j--){
        printf("%d", simpan[j]);
    }
}
