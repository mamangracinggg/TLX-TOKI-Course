#include<stdio.h>
int main(){
    int N;
    scanf("%d", &N);
    int ans = 1;
    for(int i = N; i >= 1; i-- ){
        int temp = i;
        if(i % 2 == 0){
            temp = temp / 2;
        }
        ans *= temp;
    }
    printf("%d", ans);
}
